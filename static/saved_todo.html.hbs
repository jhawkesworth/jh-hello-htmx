<div hx-target="this" hx-swap="outerHTML">
    <div><label>Thing to do</label>: {{ item.note }}</div>
    <button hx-get="/todos/{{ item.id }}/edit" class="btn btn-primary">
        Click To Edit
    </button>
</div>